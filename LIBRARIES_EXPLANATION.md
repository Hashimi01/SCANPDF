# شرح المكتبات المستخدمة في المشروع

## 📚 المكتبات الأساسية (requirements.txt)

### 1. **typer** (>=0.4.0)
**الفائدة:**
- إنشاء واجهة سطر أوامر (CLI) احترافية
- تحويل الدوال Python إلى أوامر سطر أوامر تلقائياً
- معالجة المعاملات والخيارات بسهولة

**الاستخدام في المشروع:**
- يستخدم في `i2pdf.py` لإنشاء أوامر مثل `pdf-to-text`, `pdf-summary`

---

### 2. **PyYAML** (>=6.0)
**الفائدة:**
- قراءة وكتابة ملفات YAML
- تحليل ملفات التكوين بصيغة YAML

**الاستخدام في المشروع:**
- قراءة ملفات التكوين والإعدادات

---

### 3. **Pillow** (>=10.0.0)
**الفائدة:**
- معالجة الصور (فتح، تحرير، حفظ)
- تحويل صيغ الصور
- معالجة الصور قبل OCR

**الاستخدام في المشروع:**
- معالجة صور PDF قبل استخراج النص
- تحسين جودة الصور للـ OCR

---

### 4. **PyPDF2** (>=1.26.0)
**الفائدة:**
- قراءة وكتابة ملفات PDF
- استخراج النص من PDF
- دمج وتقسيم ملفات PDF
- استخراج البيانات الوصفية (metadata)

**الاستخدام في المشروع:**
- قراءة عدد الصفحات
- استخراج البيانات الوصفية
- قراءة محتوى PDF

---

### 5. **setuptools** (>=58.1.0)
**الفائدة:**
- أدوات بناء وتوزيع حزم Python
- إدارة التبعيات
- إنشاء حزم قابلة للتثبيت

**الاستخدام في المشروع:**
- بناء وتوزيع المشروع كحزمة Python

---

### 6. **pdf2docx** (>=0.5.6)
**الفائدة:**
- تحويل ملفات PDF إلى DOCX (Word)
- الحفاظ على التنسيق والهيكل

**الاستخدام في المشروع:**
- تحويل PDF إلى Word عند الحاجة

---

### 7. **ocrmypdf** (>=16.0)
**الفائدة:**
- **الأهم**: استخراج النص من PDF باستخدام OCR (Optical Character Recognition)
- إضافة طبقة نصية لـ PDF الممسوحة ضوئياً
- دعم لغات متعددة (العربية، الفرنسية، الإنجليزية)
- استخدام Tesseract OCR في الخلفية

**الاستخدام في المشروع:**
- **مهم جداً**: استخراج النص من PDF التي لا تحتوي على طبقة نصية
- استخراج النص العربي والفرنسي من الصور الممسوحة

**ملاحظة:** يتطلب تثبيت Tesseract OCR على النظام

---

### 8. **pdfminer.six** (>=20220524)
**الفائدة:**
- استخراج النص من PDF بدقة عالية
- تحليل هيكل PDF (الصفحات، الخطوط، التنسيق)
- استخراج النص من الطبقة النصية الموجودة

**الاستخدام في المشروع:**
- استخراج النص من PDF التي تحتوي على طبقة نصية
- تحليل هيكل الصفحات
- استخراج النص العربي بدقة

---

### 9. **fastapi** (>=0.104.0)
**الفائدة:**
- إنشاء واجهات برمجة تطبيقات (API) حديثة وسريعة
- إنشاء خدمات ويب RESTful
- توثيق تلقائي للـ API

**الاستخدام في المشروع:**
- إنشاء API لاستخراج النص من PDF
- خدمة ويب لمعالجة PDF

---

### 10. **uvicorn[standard]** (>=0.24.0)
**الفائدة:**
- خادم ASGI عالي الأداء
- تشغيل تطبيقات FastAPI
- دعم WebSockets والبروتوكولات الحديثة

**الاستخدام في المشروع:**
- تشغيل خادم FastAPI
- استضافة خدمة استخراج النص

---

### 11. **python-multipart** (>=0.0.6)
**الفائدة:**
- معالجة بيانات multipart/form-data
- رفع الملفات عبر HTTP
- ضروري لـ FastAPI عند رفع الملفات

**الاستخدام في المشروع:**
- رفع ملفات PDF عبر API
- معالجة طلبات رفع الملفات

---

### 12. **requests** (>=2.31.0)
**الفائدة:**
- إرسال طلبات HTTP بسهولة
- تحميل الملفات من الإنترنت
- التعامل مع APIs

**الاستخدام في المشروع:**
- **مهم جداً**: تحميل ملفات PDF من روابط S3
- في `inspect_books.py` لتحميل PDF من الروابط

---

## 📦 المكتبات المدمجة في Python (لا تحتاج تثبيت)

### المكتبات المستخدمة في `inspect_books.py`:

1. **json** - قراءة وكتابة ملفات JSON
2. **os** - التعامل مع نظام الملفات
3. **sys** - الوصول إلى معاملات النظام
4. **tempfile** - إنشاء ملفات ومجلدات مؤقتة
5. **re** - التعامل مع التعبيرات النمطية (regex)
6. **subprocess** - تشغيل أوامر النظام
7. **time** - التعامل مع الوقت والانتظار
8. **platform** - معرفة نوع النظام (Windows/Linux/Mac)
9. **signal** - معالجة الإشارات (للإيقاف الآمن)
10. **datetime** - التعامل مع التاريخ والوقت
11. **typing** - تلميحات الأنواع (Type Hints)
12. **pathlib** - التعامل مع المسارات بطريقة حديثة

---

## 🎯 ملخص الأهمية

### **مكتبات أساسية جداً (لا يمكن الاستغناء عنها):**
1. ✅ **requests** - لتحميل PDF من الإنترنت
2. ✅ **pdfminer.six** - لاستخراج النص من PDF
3. ✅ **PyPDF2** - لقراءة بيانات PDF
4. ✅ **ocrmypdf** - لاستخراج النص من PDF الممسوحة (يتطلب Tesseract)

### **مكتبات مهمة:**
5. ⭐ **typer** - لواجهة سطر الأوامر
6. ⭐ **fastapi + uvicorn** - لخدمة API (إن وجدت)

### **مكتبات مساعدة:**
7. 📌 **Pillow** - لمعالجة الصور
8. 📌 **pdf2docx** - لتحويل PDF إلى Word
9. 📌 **PyYAML** - لملفات التكوين

---

## ⚠️ ملاحظات مهمة

1. **ocrmypdf** يتطلب تثبيت **Tesseract OCR** على النظام:
   - Windows: تحميل من [هنا](https://github.com/UB-Mannheim/tesseract/wiki)
   - Linux: `sudo apt-get install tesseract-ocr tesseract-ocr-ara tesseract-ocr-fra`
   - Mac: `brew install tesseract tesseract-lang`

2. **pdfminer.six** و **PyPDF2** يكملان بعضهما:
   - `pdfminer.six`: دقة أعلى في استخراج النص
   - `PyPDF2`: أسرع وأبسط للعمليات الأساسية

3. جميع المكتبات متوافقة مع Python 3.8+









