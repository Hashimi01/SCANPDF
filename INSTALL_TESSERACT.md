# كيفية تثبيت Tesseract OCR على Windows

## 📥 خطوات التثبيت:

### 1. تحميل Tesseract OCR
- **رابط التحميل**: https://github.com/UB-Mannheim/tesseract/wiki
- اختر أحدث إصدار لـ Windows (مثلاً: `tesseract-ocr-w64-setup-5.x.x.exe`)

### 2. تثبيت البرنامج
1. شغّل ملف التثبيت
2. **مهم جداً**: أثناء التثبيت، تأكد من اختيار:
   - ✅ **Arabic** (العربية)
   - ✅ **French** (الفرنسية)
   - ✅ **English** (الإنجليزية)
3. اختر مسار التثبيت (مثلاً: `C:\Program Files\Tesseract-OCR`)

### 3. إضافة Tesseract إلى PATH
1. افتح **System Properties** (خصائص النظام)
2. اضغط على **Environment Variables** (متغيرات البيئة)
3. في **System Variables**، ابحث عن **Path** واضغط **Edit**
4. اضغط **New** وأضف مسار Tesseract:
   ```
   C:\Program Files\Tesseract-OCR
   ```
5. اضغط **OK** لحفظ التغييرات

### 4. التحقق من التثبيت
افتح Terminal جديد واكتب:
```bash
tesseract --version
```

يجب أن ترى إصدار Tesseract (مثلاً: `tesseract 5.x.x`)

### 5. التحقق من اللغات المثبتة
```bash
tesseract --list-langs
```

يجب أن ترى:
- `ara` (العربية)
- `fra` (الفرنسية)
- `eng` (الإنجليزية)

---

## ⚠️ ملاحظات مهمة:

1. **أعد تشغيل Terminal** بعد التثبيت حتى يتم تحديث PATH
2. إذا لم يعمل الأمر `tesseract`، أعد تشغيل الكمبيوتر
3. تأكد من تثبيت حزم اللغات (Arabic, French) أثناء التثبيت

---

## 🔍 بعد التثبيت:

بعد تثبيت Tesseract OCR، سيعمل `ocrmypdf` بشكل صحيح وستتمكن من:
- ✅ استخراج النص من PDF الممسوحة ضوئياً
- ✅ استخدام OCR للعربية والفرنسية
- ✅ الحصول على نتائج أفضل من الطبقة النصية فقط

---

## 📝 بديل سريع (للتجربة فقط):

إذا كنت تريد تجربة OCR بدون تثبيت، يمكنك استخدام Docker:
```bash
docker run --rm -v "%cd%":/workspace tesseractshadow/tesseract4re tesseract input.pdf output
```

لكن الأفضل هو تثبيت Tesseract مباشرة على النظام.









